corresp:
  n_features: 200
  distance_threshold_px: 1
  distances_ratio: 0.5
  reprojection_error_px: 1
  debug_matches: true
  debug_epipolar: true
  debug_markers: true
  debug_projective_error: true
  debug_distances: true
  # triangulation method can be so-called primitive or svd
  triangulation_method: "svd"

log_filenames:
  debug_dump_to_files: true
  #
  absolute_path_prefix: "/home/mrs/git/uav_camera_localization/documents/"
  distance: 6.6
  # if true - plane will be generated from distance (parallel to yz plane), otherwise it will be estimated
  generate_plane: true
#  rms_reprojection_error: "_rms_repro_err.csv"
#  total_reprojection_error: "_total_repro_err.csv"
  distance_camera_plane: "_plane_distance.csv"
  distance_each_pt_to_plane: "_plane_pts_distance.csv"

